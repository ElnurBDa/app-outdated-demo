# VulnWatch remediation

**CVE / ID:** `CVE-2024-45590`
**Package:** `body-parser`
**Suggested fixed version:** `1.20.3`
**Finding id:** `6a2042eed173d9f902a5a322`

## Automated suggestion

### Remediation for `body-parser` (CVE-2024-45590)

**Package:** `body-parser`
**Affected Version:** `1.18.2`
**Upgrade Target:** `1.20.3`

**Description:**
A vulnerability in `body-parser` versions prior to 1.20.3 allows for a denial of service attack when parsing malformed JSON.

**Manifest Files to Update:**
* `package.json`
* `package-lock.json`
* `yarn.lock` (if applicable)
* `bun.lockb` (if applicable)

**Remediation Steps:**
1. Update the `body-parser` dependency in your `package.json` to version `1.20.3` or higher.
   ```json
   "dependencies": {
     "body-parser": "1.20.3"
   }
   ```
2. Run your package manager's install command to update the lock file:
   * `npm install`
   * `yarn install`
   * `bun install`

**Verification:**
Run your project's test suite and build process to ensure compatibility and functionality.

## Code changes

- Patched: `package.json`, `package-lock.json`

---
*Opened by [VulnWatch](https://github.com/). Review, test, and adjust versions before merge.*
